# dotfiles

Dotfiles managed by [Chezmoi](https://www.chezmoi.io)

## How to apply?

```sh
sh -c "$(curl -fsLS get.chezmoi.io/lb)" -- init --apply YoungseokCh
```

On Linux, choose `system` to install packages with `sudo`, or `user` to install
Neovim under `~/.local` without `sudo`.
